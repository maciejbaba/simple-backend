# Simple version of the backend

In order to run it:

```bash
git clone https://github.com/maciejbaba/simple-backend
cd simple-backend
cd be
npm install
npm run dev &
cd ../fe
npm install
npm run dev
```
